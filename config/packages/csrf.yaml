# Enable stateful CSRF protection for forms and logins/logouts.
# Login/logout tokens must stay session-backed because the forms use csrf_token().
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection: true
